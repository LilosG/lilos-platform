# Search Console Connector

Search Console uses the shared OAuth, connection, synchronization, checkpoint, and secret-reference boundaries. Domain and URL-prefix properties are discovered then explicitly confirmed. Query/page dimensions, provider delay, partial rows, aggregation limits, freshness, and quality are retained. CI uses a deterministic adapter; live validation requires an approved Google project.
