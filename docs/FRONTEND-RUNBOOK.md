# Frontend Operational Runbook

Session expiry returns to generic authentication guidance without losing secrets. Authorization loss removes affected actions after refresh while backend enforcement remains authoritative. Version conflicts retain form input for deliberate reload. Provider degradation, timeout, partial data, runtime pause, and ambiguous external actions display safe state and correlation references rather than false success.
