# Feature flags

Flags are immutable effective-dated revisions at organization or owned-location scope. Resolution
prefers current location scope and exposes the winning record, scope, version, purpose, risk, and
review date.

Flags stage optional behavior only. They cannot grant identity, membership, permission,
entitlement, readiness, approval bypass, lifecycle bypass, or external action. They contain no
arbitrary targeting expressions or secrets.
