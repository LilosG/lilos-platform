# Operational Dashboard Catalog

Owned dashboards cover API/auth, PostgreSQL, queues/workers, scheduler/workflows, notifications, providers/connections, synchronization, AI usage/cost, product operations, backups, deployments, incidents, SLOs, and error budgets. Every view identifies environment, service, product, provider, and workflow where safe. Tenant context is available only to authorized operators and never includes customer content.
